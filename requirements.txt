# Notebook/research runtime only. The production CLI (shanzhai.cli) is
# stdlib-only and needs no dependencies.
# Versions verified to install as wheels on Python 3.14 (2026-08-11).
python-binance==1.0.29
matplotlib==3.11.1
numpy==2.5.2
pandas==3.0.5
nbconvert==7.17.1
ipykernel==7.3.0
